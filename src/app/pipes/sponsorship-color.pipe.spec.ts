import { SponsorshipColorPipe } from './sponsorship-color.pipe';

describe('SponsorshipColorPipe', () => {
  it('create an instance', () => {
    const pipe = new SponsorshipColorPipe();
    expect(pipe).toBeTruthy();
  });
});
